# PR record reconciliation Hotfix next actions

- #181/#182 branch가 synchronize되면 `pr-template-drift` workflow가 check rollup에 붙는지 확인한다.
- #181은 Draft 상태이므로 review-ready 전 scope와 generated output policy를 재확인한다.
- #182를 merge/finalize하려면 별도 명시 지시로 진행한다.

