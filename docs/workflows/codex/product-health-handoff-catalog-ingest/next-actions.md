# Product Health handoff catalog ingest next actions

- M6 focused regression suite와 harness validation 결과를 `quality.md`에 추가한다.
- 필요하면 UI에서 `CatalogMetadata.metrics.input_total_bytes`를 5GB evidence로 표시하는 별도 M1 slice를 진행한다.
