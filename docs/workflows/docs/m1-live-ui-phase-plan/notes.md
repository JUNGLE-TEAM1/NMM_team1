# M1 live UI Phase plan 작업 메모

- M1 shell은 이미 있지만 Week2 M5/M6 live API 연결은 아직 없다.
- 이번 문서는 M1이 지금 할 수 있는 live UI 작업을 API client, run, catalog, AI Query, demo polish로 분해한다.
- Phase 1은 가장 작고 안전한 API client 추가다.
- Phase 2~4는 이미 존재하는 backend API를 소비한다.
- Phase 5는 발표 클릭 흐름 polish이며 fake success를 만들면 안 된다.
