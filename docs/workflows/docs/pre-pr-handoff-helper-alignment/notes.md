# Notes

## Context

- Context Budget mode: Escalate Read.
- Earliest impacted layer: Git sync policy / harness behavior.
- Report basis: `docs/reports/harness-post-merge-change-audit.md`.

## Decisions During Work

- H-1은 이번 Phase에서 처리한다.
- H-3은 evidence 권장 위치를 명확히 하고 fixture를 보강한다.
- H-2 `PR 진행` 승인 범위 분리는 더 큰 command vocabulary 결정이 필요해 deferred로 둔다.
- rescue stash 삭제는 destructive cleanup이라 이번 Phase에서 실행하지 않는다.
