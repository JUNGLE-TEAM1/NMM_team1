# Week2 M6 RAG scope 보강 노트

- 사용자는 리뉴얼된 M6 Semantic/RAG/AI Query 문서에서 RAG가 사라진 것처럼 보인다고 지적했다.
- 확인 결과 원래 Week2 결정에는 `M6 RAG·AI Query`가 있었고, ver2 책임 문서에도 `Semantic/RAG/AI Query`가 남아 있었다.
- 다만 분석 대표 path 문서와 팀 공유 요약에서 `AI Query` 중심으로 축약되어 RAG가 약하게 보였다.
- 보강 기준은 full RAG 확대가 아니라 `CatalogMetadata` 기반 semantic retrieval, dataset selection evidence, RAG-lite grounding이다.
