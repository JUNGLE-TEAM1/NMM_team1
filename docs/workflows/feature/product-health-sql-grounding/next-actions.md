# PH-DATA-5 다음 행동

1. M1 화면에서 Product Health CTA가 `/api/week2/ai/query` 대표 질문을 호출하도록 연결한다.
2. M1 evidence panel에 dataset id, SQL, risk row, processed input bytes를 표시한다.
3. Product title/source title 품질을 데모용으로 더 다듬을지 결정한다.
4. 후속으로 category label과 scenario bucket 중심 요약 문장을 보강한다.
