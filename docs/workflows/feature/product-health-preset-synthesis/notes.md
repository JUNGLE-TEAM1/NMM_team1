# Product Health Preset Synthesis 노트

- 기존 합성 스크립트는 약 3초 내외로 실행되는 row-limited smoke synthesis다.
- 실행 결과는 ignored local `data/local_sources/product_health/*` artifact를 갱신한다.
- `Product Health Demo preset` panel은 Gold Dataset 화면에 배치했다. 현재 IA에서 Gold 결과를 재생성한 뒤 Run/Catalog/AI Query로 이어지는 위치가 가장 자연스럽기 때문이다.
