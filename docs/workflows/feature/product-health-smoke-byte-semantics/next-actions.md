# PH-DATA-2C 다음 행동

1. PH-DATA-3에서 실제 처리 bytes 측정 방식을 정한다.
2. PH-DATA-3 run summary에서 `input_total_bytes`와 `processed_input_total_bytes`를 같은 값으로 맞춘다.
3. M1 evidence label은 "prepared source bytes"와 "processed input bytes"를 분리해서 표시한다.
