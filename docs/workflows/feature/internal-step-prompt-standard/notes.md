# Internal step prompt standard 노트

## 진행 메모

- Added standard storage format for internal Step prompts before M2.

## 결정

- Keep Step prompts in `plan.md`; consider `steps.md` only if plan files become too large later.

## 열린 질문

- Should M2 use a full Step list immediately? Recommended yes, in `plan.md`.

## 링크 / 증거

- Issue #18 was created by `scripts/start-workflow.sh` as the team default.
