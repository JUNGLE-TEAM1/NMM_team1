# Dataset section reframe 노트

## 진행 메모

- 2026-06-29: 기존 `데이터 통합` 중심 prototype을 `데이터셋` 생성/관리 화면으로 재정의하기로 함.
- 기존 `data-integration-*` workspace는 삭제하지 않고 prototype evidence로 보존한다.
- 2026-06-29: nav/page title/top search/wizard header/review wording을 `데이터셋` 중심으로 조정함.
- 2026-06-29: 브라우저에서 `/dataset` 첫 화면, source 선택, Transform 진입을 확인함.

## 결정

- 이 Phase는 명칭/목적/CTA copy만 정리하고 Source/Target 선택 modal은 다음 Phase로 분리한다.

## 열린 질문

- 없음. 구현 시 copy 톤은 AWS/XFlow처럼 짧고 설정 중심으로 맞춘다.

## 링크 / 증거

- `docs/08-development-workflow.md`의 `Dataset Creation UX Reframe Queue`
