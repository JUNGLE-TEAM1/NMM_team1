# Dataset section reframe 다음 행동 메뉴

사람이 다음 협업 선택을 쉽게 고를 수 있도록 현재 상태와 선택지를 기록한다.

## Current State / 현재 상태

- State: implemented
- Summary: `데이터 통합` 화면을 `데이터셋` 화면으로 재정의하는 첫 Phase가 구현/검증되었다.

## Recommended Next Action / 권장 다음 행동

- 다음 Phase인 `feature/dataset-create-type-choice`로 진행한다.
- Reason: 화면의 이름과 목적이 정리되었으므로 이제 `데이터셋 생성`에서 Source/Target 선택 분기를 추가할 수 있다.

## Options / 선택지

1. `feature/dataset-create-type-choice` 구현을 시작한다.
2. D-1 copy를 추가 보정한다.
3. Source Dataset wizard 범위를 먼저 재검토한다.
4. 이 workspace를 멈춘다.

## Waiting On Human / 사람 응답 대기

- 다음 지시를 기다린다.

## Last User Choice / 마지막 사용자 선택

- 

## Next AI Action / 다음 AI 행동

- option 1이면 `feature/dataset-create-type-choice` workspace 범위로 이동한다.
- option 2이면 D-1 copy를 보정하고 재검증한다.
- option 3이면 `feature/source-dataset-create-wizard` plan을 재검토한다.
- option 4이면 pause reason을 `notes.md`에 기록한다.
