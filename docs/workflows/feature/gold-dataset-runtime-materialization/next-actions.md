# Gold dataset runtime materialization next actions

## 바로 다음

- C-29 `feature/jobs-runs-runtime-integration` 진행.
- Connection/Source Snapshot/Silver Materialization/Gold Build evidence를 Jobs/Runs 화면에서 같은 언어로 정렬한다.

## 보류

- Gold runner에서 실제 MinIO upload opt-in 실행.
- production-grade join/aggregate/risk scoring recipe engine.
- Catalog publish와 AI Query runtime Gold E2E는 C-30에서 진행한다.
