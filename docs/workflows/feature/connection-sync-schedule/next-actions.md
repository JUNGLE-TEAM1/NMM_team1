# Connection sync schedule 다음 작업

- 구현 phase에서 `ExternalConnectionCreate/Record`와 SQLite store에 sync metadata를 추가한다.
- External Connection wizard configure/review/list card에 sync mode와 schedule hint를 표시한다.
- 기존 Gold/Silver job schedule 문구는 processing/build schedule로 유지한다.
