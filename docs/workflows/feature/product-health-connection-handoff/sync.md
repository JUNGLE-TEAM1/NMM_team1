# PH-DATA-2B Git Sync

## Branch

- Current branch: `feature/external-connection-persistence`

## 상태

- branch가 remote보다 뒤처져 있으므로 merge/pull/rebase는 사람 확인 없이는 수행하지 않는다.
- 이번 Phase는 local handoff 산출물과 문서만 보정한다.

## 주의

- `data/` 아래 generated artifact는 commit하지 않는다.
- PR #297/#298은 아직 머지되지 않았으므로 실제 사이트 persistence가 있다고 가정하지 않는다.
