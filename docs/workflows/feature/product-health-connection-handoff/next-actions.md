# PH-DATA-2B 다음 행동

1. PH-DATA-3에서 5GB evidence run을 수행할 때 같은 source dataset id를 유지한다.
2. PR #297이 머지되면 `conn_taxi_postgres` 실제 registration id와 handoff id를 맞춘다.
3. PR #298이 머지되면 Dataset Inventory 화면에서 handoff source dataset 목록을 표시할 수 있는지 확인한다.
4. PH-DATA-4에서 source handoff와 catalog handoff를 M5 CatalogMetadata ingestion으로 연결한다.
