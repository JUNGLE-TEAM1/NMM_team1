# Dataset management actions 보고서

## Short Report / 짧은 보고

- Type: feature
- Date: 2026-06-30
- Changed: External Connection, Silver Dataset, planned Gold/Target Dataset draft에 update/delete API와 목록 상세/수정/삭제 action을 추가했다. Jobs `Dataset 편집`은 해당 Silver/Gold 수정 modal을 바로 연다. Source Dataset 생성의 Connection 선택에서 mock connection을 제거하고 저장된 External Connection만 표시한다. 생성 wizard footer는 첫 단계 `목록으로` + `다음`, 이후 `뒤로가기` + `다음/저장`으로 통일했다. UX hotfix로 Source 생성의 애매한 유형 변경 버튼을 제거하고, Gold 생성 용어/라벨과 Product Health AI 예시, `/` canonical route를 정리했다. 누락된 `AI Query` sidebar nav를 복구했고, Query 화면의 readiness/demo status 카드와 중복 empty card를 제거한 뒤 답변 제목/근거 요약/SQL 실행 정보를 분리했다. AI Query API summary는 raw `dataset/run_id/row_count/schema` 문자열을 붙이지 않고 사람이 읽는 결론만 반환하게 변경했다. Job schedule modal의 schedule 카드와 compact 안내 박스 텍스트가 18px grid column으로 밀려 세로로 깨지는 CSS를 보정했다. 작업 하위 메뉴에 Connection Sync Jobs를 추가하고, Connection/Silver/Gold 작업 액션을 `수동 실행`, `Schedule 수정`, `Connection 편집` 또는 `Dataset 편집`으로 통일했다. Source/Silver/Gold Dataset 생성 Review 단계에 강조 카드와 lineage strip을 추가해 최종 확인 화면을 보강했다. Gold Dataset Process 단계는 Inputs -> Processing -> Output 다이어그램으로 교체해 Silver 입력, recipe sequence, Gold/Catalog output을 한눈에 보이게 했다. Job 카드는 한 줄 detail 대신 input/output/rules/schedule fact grid로 표시하게 정리했다. Source/Silver/Gold Dataset 목록 카드도 fact grid와 layer별 variant로 정리했다. CatalogDataset management boundary는 Gold Dataset 목록에서 제거하고 별도 `데이터 카탈로그` 메뉴/화면으로 이동했다. 데이터 카탈로그 화면은 등록 dataset 목록, 선택 상세, schema preview, AI Query handoff 중심으로 재구성하고 관리 정책은 접힌 섹션으로 내렸다. 실행 기록은 Gold 전용 화면에서 `전체/Connection/Silver/Gold/실패` 필터가 있는 공통 작업 로그 화면으로 재구성했다. 데모 정리를 위해 중복 Product Health Gold run/catalog row를 제거하고 최신 1쌍만 남겼다.
- Verified: backend focused tests 26개와 Hotfix focused tests 27개, frontend build, browser smoke에서 목록 action 노출과 Jobs 직접 편집 진입을 확인했다. Mock connection 제거와 생성 wizard footer 일관화 후 frontend build를 재확인했고, Source/Silver/Gold Dataset 생성 및 External Connection 생성 첫 단계 footer를 브라우저에서 확인했다. UX hotfix 후 `/`, Source 생성 2단계 라벨, Gold 생성 1단계 용어, AI 도우미 예시를 브라우저에서 확인했다. `AI Query` sidebar nav 클릭 후 `/query` 화면 진입을 확인했다. Query 화면 정리 후 frontend build와 `/query` 초기/질문 실행 smoke를 확인했다. AI Query summary 정리 후 backend focused tests 16개, API smoke, browser smoke에서 raw evidence 문자열 미노출을 확인했다. Job schedule modal 텍스트 깨짐 수정 후 frontend build와 `/jobs/silver-transform` modal browser smoke를 확인했다. Jobs 실행 모델 정리 후 frontend build와 `/jobs/connection-sync`, `/jobs/silver-transform`, `/jobs/gold-build` browser smoke를 확인했다. Dataset Review polish 후 Source/Silver/Gold 생성 wizard Review 단계 browser smoke를 확인했다. Gold Process 다이어그램 보강 후 frontend build와 Process 단계 browser smoke를 확인했다. Job card fact grid 보강 후 세 Jobs 화면 browser smoke를 확인했다. Dataset card fact grid 보강 후 Source/Silver/Gold 목록과 Job 카드 회귀 browser smoke를 확인했다. Catalog 분리 후 frontend build와 browser smoke에서 `/datasets/gold` 정책 패널 미노출, `/catalog` 정책 패널 노출을 확인했다. Catalog UI 재구성 후 browser smoke에서 목적 카드, CatalogDataset 카드 2개, 선택 상세, schema preview, 접힌 정책 섹션을 확인했다. 실행 기록 재구성 후 browser smoke에서 공통 작업 로그 필터, Gold run 2건, Connection empty filter, Gold filter, Airflow/Spark/Kafka 미노출을 확인했다. 데모 데이터 정리 후 `/runs` 1건, `/catalog` 1건, AI Query selected dataset/evidence가 동일한 최신 CatalogDataset/run을 가리키는 것을 확인했다.
- Remaining: registered CatalogDataset 수정/삭제, cascade delete, 실제 file/object/runner 삭제는 제외 범위로 남겼다.
- Next context: Dataset 목록 관리 액션과 Jobs 편집 직접 진입이 맞춰졌다. Source 삭제는 Silver 참조 시 차단되고, Silver name 변경은 Target draft 참조 시 차단된다. Gold rename은 `gold_output`도 함께 맞춘다.
- Risk: 삭제는 metadata-only이며 참조 중인 row와 registered CatalogDataset을 건드리지 않는다.
