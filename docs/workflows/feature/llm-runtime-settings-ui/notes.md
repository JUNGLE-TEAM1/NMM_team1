# 진행 메모

## 2026-06-30

- `.env.local`은 local-only secret 파일로 유지하고 Git에는 포함하지 않는다.
- PR 분리 과정에서 LLM local env 설정은 PR #308, MongoDB Source Dataset seed는 stacked PR #309로 나눴다.
- strict harness가 요구하는 workspace 필수 파일과 decision handoff를 보강했다.
