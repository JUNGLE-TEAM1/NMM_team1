# Runtime connection verification report 다음 액션

1. C-24 `feature/runs-runtime-panel-restore`
   - `/runs` 화면에서 Airflow/Spark/Kafka readiness/evidence panel을 복구한다.
2. C-25 `feature/external-connection-runtime-checks`
   - PostgreSQL/MongoDB/S3/Kafka connection test API를 secret-ref 경계로 구현한다.
3. C-31 `feature/deep-browser-runtime-e2e`
   - `Connection -> Source -> Silver -> Gold -> Run -> Catalog -> AI Query`를 브라우저에서 끝까지 검수한다.
