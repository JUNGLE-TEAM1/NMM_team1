# Target dataset job draft 다음 행동 메뉴

1. Recommended: C-3 PR을 review/merge한 뒤 C-4 `feature/target-dataset-run-handoff`에서 저장된 `job_definition`을 M5 run 생성으로 넘긴다.
2. Alternative: PR review에서 job definition shape 보강이 필요하면 C-3 안에서 schema 필드를 보정한 뒤 C-4로 넘어간다.
3. Stop: 실행 버튼이나 run history를 C-3에 추가하려는 요구가 생기면 scope creep로 보고 C-4 항목으로 분리한다.
