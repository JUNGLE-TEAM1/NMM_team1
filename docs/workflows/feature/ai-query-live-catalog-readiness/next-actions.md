# AI query live catalog readiness 다음 작업

- 브라우저 smoke에서 published Gold Dataset 생성 후 `/query` readiness panel이 live catalog를 표시하는지 확인한다.
- 이후 필요하면 `/catalog-detail`이 live CatalogDataset id를 받을 수 있게 확장한다.
